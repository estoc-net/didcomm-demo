{
  "name": "estoc-didcomm-demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A see-through DIDComm v2 messenger: chat between browser profiles through a mediator, with every envelope layer of every message open for inspection.",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "vue-tsc -b",
    "e2e": "node scripts/e2e.mjs"
  },
  "dependencies": {
    "@noble/curves": "^1.9.0",
    "@noble/hashes": "^1.8.0",
    "bs58": "^6.0.0",
    "didcomm": "^0.4.1",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "playwright-core": "^1.62.1",
    "typescript": "~5.8.0",
    "vite": "^6.3.0",
    "vue-tsc": "^2.2.0"
  }
}
